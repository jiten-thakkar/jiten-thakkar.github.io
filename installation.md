* Install bundler and jekyll --> `gem install --user-install bundler jekyll`
* Install Dependencies --> `bundle install`
* Run Server --> `bundle exec jekyll serve` 
* Website --> localhost:4000
* Admin --> localhost:4000/admin
